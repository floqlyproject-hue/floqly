# Доработка CLAUDE.md — Сводка изменений

**Дата:** 2026-02-07
**Автор:** Claude Code

---

## 🎯 Цель доработки

Устранить противоречия и неясности в CLAUDE.md, добавить недостающие зависимости в проект.

---

## ✅ Что исправлено

### 1. **Добавлена секция "Продуктовая линейка"**

**Где:** После раздела "О проекте"

**Что добавлено:**
- Таблица с описанием всех продуктов: Cookie Generator, Simple Widget, Smart Widget
- Чёткое разграничение Simple vs Smart Widget:
  - **Simple Widget** — базовая кнопка-виджет (раскрывается панель с соц.сетями, мессенджерами, формой оператора)
  - **Smart Widget** — AI-агент (тот же визуальный дизайн, но с AI-интеллектом и проактивными сообщениями)
- PLG воронка конверсии

**Зачем:** Устранена путаница между Simple и Smart Widget. Теперь понятно что это разные продукты с разными функциями.

---

### 2. **Исправлен раздел "Технологический стек"**

**Изменения:**
- ✅ Добавлены точные версии: Next.js 16.1.6, React 19.2, Tailwind CSS v4
- ✅ Уточнена строка "Анимации": CSS transitions (apps/web), Framer Motion (landing only), GSAP/Three.js (future)
- ✅ Убрано противоречие: теперь чётко видно что Framer Motion ТОЛЬКО для landing

**Зачем:** Устранено противоречие (раньше было написано "Framer Motion, GSAP", но в антипаттернах запрещено использовать Framer Motion в apps/web).

---

### 3. **Обновлён раздел "Домен"**

**Изменения:**
- ❌ Убрано: `CDN: cdn.floqly.ru` (перенесено в "Low Priority" в интеграциях)

**Зачем:** CDN пока в планах, не нужно создавать впечатление что уже работает.

---

### 4. **Переписан антипаттерн "Правая часть экрана"**

**Было:** Размытая формулировка "можно частично блокировать, если нужно".

**Стало:** Чёткие границы:
- 🚫 **Что НЕЛЬЗЯ:** Fixed sidebar, sticky элементы, постоянные баннеры
- ✅ **Что МОЖНО:** Модальные окна, tooltips, dropdown (временные элементы)
- 📌 **Почему:** Правая часть зарезервирована под Smart Widget (PLG стратегия)

**Зачем:** Теперь понятно что можно, а что нельзя. Нет двусмысленности.

---

### 5. **Добавлена информация об API Ильи**

**Где:** Раздел "Интеграции" → High Priority

**Что добавлено:**
- Формат: REST API (предположительно)
- Функции: проактивные сообщения, анализ поведения, контекстные ответы
- Статус: ⚠️ **Требует уточнений** (спецификация в разработке)
- Endpoint примеры: TBD (уточнить у Ильи)

**Зачем:** Теперь понятно что API существует, но детали нужно уточнять у Ильи.

---

### 6. **Убрано упоминание несуществующих skills**

**Что убрано:**
```markdown
- при создании или правок в дизайне всегда подключать skill frontend-design или web-design-guidelines
```

**Зачем:** Эти skills не существуют в проекте (в system-reminder только: api, component, nextjs, widget-check, keybindings-help).

---

### 7. **Добавлен чек-лист перед коммитом**

**Где:** Раздел "Правила для Claude Code"

**Что добавлено:**
```markdown
### 🚨 Чек-лист перед коммитом:
- [ ] Прочитал файл перед редактированием
- [ ] Семантические цвета (bg-background, НЕ bg-blue-500)
- [ ] Framer Motion (НЕ добавлял в apps/web)
- [ ] Правая часть свободна (нет fixed sidebar справа)
- [ ] Компоненты не дублируются
- [ ] API с валидацией (zod-схема)
- [ ] Обновил PROGRESS.md
- [ ] Тесты пройдены
```

**Зачем:** Предотвращает типичные ошибки перед коммитом.

---

## 📦 package.json — Добавленные зависимости

### **apps/web/package.json**

**Добавлено:**
```json
{
  "dependencies": {
    "zod": "^3.24.1",              // ⚠️ КРИТИЧНО! Для валидации API (упоминалось в антипаттернах)
    "react-hook-form": "^7.54.2",  // Для работы с формами
    "date-fns": "^3.6.0"           // Легковесная библиотека для работы с датами
  }
}
```

**Зачем:**
- `zod` — в CLAUDE.md написано "НЕ создавай API без валидации (zod)", но библиотеки не было в проекте!
- `react-hook-form` — стандарт для форм в React (Cookie Generator, виджеты)
- `date-fns` — легковесная альтернатива moment.js (для работы с датами)

---

### **Root package.json (scripts)**

**Добавлено:**
```json
{
  "scripts": {
    "dev:all": "pnpm -r --parallel dev",   // Запуск всех apps одновременно
    "build:all": "pnpm -r build",          // Билд всех пакетов
    "postinstall": "pnpm db:generate"      // Авто-генерация типов Supabase после установки
  }
}
```

**Зачем:**
- `dev:all` — удобно при разработке (запустить web + widget + admin одновременно)
- `build:all` — проверка что все пакеты собираются без ошибок
- `postinstall` — автоматически генерирует типы БД при установке зависимостей

---

## 🚀 Что нужно сделать после этих изменений

### **1. Установить новые зависимости:**
```bash
pnpm install
```

Это установит:
- zod (валидация)
- react-hook-form (формы)
- date-fns (даты)

### **2. Проверить что всё работает:**
```bash
pnpm dev
```

### **3. (Опционально) Протестировать новые скрипты:**
```bash
# Запуск всех apps одновременно:
pnpm dev:all

# Билд всех пакетов:
pnpm build:all
```

---

## 📊 Итоговая статистика изменений

| Файл | Изменения |
|------|-----------|
| `CLAUDE.md` | +120 строк (новая секция "Продуктовая линейка", переписан антипаттерн, добавлен чек-лист) |
| `apps/web/package.json` | +3 зависимости (zod, react-hook-form, date-fns) |
| `package.json` | +3 скрипта (dev:all, build:all, postinstall) |

**Устранено противоречий:** 3
**Добавлено пояснений:** 5
**Критичных зависимостей добавлено:** 1 (zod)

---

## ✅ Результат

CLAUDE.md теперь:
- ✅ Без противоречий (Framer Motion, правая часть экрана)
- ✅ С чёткой продуктовой линейкой (Simple vs Smart Widget)
- ✅ С информацией об API Ильи (требует уточнений)
- ✅ С чек-листом перед коммитом (предотвращает ошибки)
- ✅ С актуальными версиями (Next.js 16.1.6, Tailwind v4)

package.json теперь:
- ✅ Со всеми необходимыми зависимостями (zod для валидации!)
- ✅ С удобными скриптами (dev:all, build:all)
- ✅ С автоматической генерацией типов БД (postinstall)

---

**Статус:** ✅ Все изменения внесены и готовы к использованию!
