# 🛠️ MCP Серверы — Гайд для Никиты

> Дата создания: 2026-02-09
> Установленные MCP: Memory Bank, Sequential Thinking, FileSystem, Context7

---

## 📦 Установленные MCP серверы

Файл конфигурации: `~/AppData/Roaming/Claude/claude_desktop_config.json`

| MCP | Для чего | Как работает |
|-----|----------|--------------|
| **Memory Bank** | Долгосрочная память между сессиями | Автоматически |
| **Sequential Thinking** | Структурирование сложных задач | Автоматически |
| **FileSystem** | Быстрая работа с файлами | Автоматически |
| **Context7** | Актуальная документация библиотек | По запросу |

---

## 🧠 Memory Bank MCP — Долгосрочная память

### Как это работает:

**Автоматически:**
- Claude запоминает важные факты о проекте, твоих предпочтениях, решениях
- Память сохраняется между сессиями (даже после перезапуска)
- Не нужно каждый раз объяснять "а помнишь, мы..."

**Что Claude запоминает:**
1. **Технические решения:**
   - "В проекте используем Tailwind v4, не CSS-in-JS"
   - "Framer Motion только для landing, не для инструментов"
   - "Simple Widget разрабатываем ПЕРВЫМ, потом переносим в Smart"

2. **Дизайн-предпочтения:**
   - "Никита любит минимализм в стиле Linear/Vercel"
   - "Правая часть экрана зарезервирована под Smart Widget"
   - "Не использовать backdrop-blur в инструментах"

3. **Бизнес-контекст:**
   - "Целевая аудитория: малый и средний бизнес в РФ"
   - "PLG модель: бесплатные инструменты → конверсия в платный виджет"

4. **Личные предпочтения:**
   - "Никита предпочитает краткие ответы без воды"
   - "Коммиты делать по ходу работы, не в конце сессии"

### Как ты будешь работать с этим:

**✅ НИЧЕГО НЕ НУЖНО ДЕЛАТЬ!** Claude автоматически:
- Запоминает важные факты во время разговора
- Использует память в следующих сессиях
- Обновляет устаревшие факты

**Пример:**
```
Сессия 1:
Никита: "Мне не нравится этот оттенок синего, давай сделаем более насыщенный"
Claude: [запоминает: Никита предпочитает насыщенные цвета]

Сессия 2 (через неделю):
Никита: "Создай кнопку для виджета"
Claude: "Сделал кнопку с насыщенным синим (помню твоё предпочтение)"
```

### Как проверить что Memory Bank работает:

После установки перезапусти Claude Code, потом спроси:
```
"Что ты помнишь обо мне и проекте Floqly?"
```

Claude выведет список сохранённых фактов.

### Как очистить память (если нужно):

```bash
# Удалить все воспоминания
rm -rf ~/.claude/memory/
```

---

## 🧩 Sequential Thinking MCP — Структурированное мышление

### Как это работает:

**Автоматически включается для сложных задач:**
- Планирование архитектуры
- Рефакторинг большого количества файлов
- Исправление сложных багов
- Разработка новых фич

**Что делает:**
1. Разбивает задачу на подзадачи
2. Выполняет их последовательно
3. Проверяет результат после каждого шага
4. Меньше ошибок → меньше переделок → экономия токенов

**Пример:**
```
Задача: "Добавить темную тему в Cookie Generator"

Без Sequential Thinking:
- Claude сразу начинает менять стили
- Забывает про шаг 3, 4, 5
- Приходится переделывать → лишние токены

С Sequential Thinking:
[Шаг 1] Проверить текущие стили
[Шаг 2] Добавить CSS переменные для темной темы
[Шаг 3] Обновить компоненты
[Шаг 4] Проверить в обеих темах
[Шаг 5] Закоммитить
→ Всё работает с первого раза!
```

### Как ты будешь работать с этим:

**✅ НИЧЕГО НЕ НУЖНО ДЕЛАТЬ!** Claude автоматически использует при сложных задачах.

Ты можешь явно попросить:
```
"Давай подумаем пошагово над архитектурой Smart Widget"
"Разбей эту задачу на этапы"
```

---

## 📂 FileSystem MCP — Быстрая работа с файлами

### Как это работает:

**Автоматически:**
- Быстрое чтение директорий (вместо `ls`)
- Batch чтение файлов (несколько за раз)
- Поиск файлов (вместо `find`)

**Что это даёт:**
- Меньше промежуточных команд
- Быстрее навигация по проекту
- Экономия токенов (~3-5k на сессию)

**Пример:**
```
Без FileSystem MCP:
[Bash] ls apps/web/src/components
[Bash] ls packages/ui/src
[Bash] find . -name "Button*"
→ 3 команды, 3 запроса

С FileSystem MCP:
[FileSystem] Покажи все компоненты в apps/web и packages/ui
→ 1 запрос, все данные сразу
```

### Как ты будешь работать с этим:

**✅ НИЧЕГО НЕ НУЖНО ДЕЛАТЬ!** Claude автоматически использует вместо Bash команд.

---

## 📚 Context7 MCP — Актуальная документация

### Как это работает:

**По запросу:**
- Ты или Claude запрашиваете актуальную документацию библиотеки
- Context7 скачивает последнюю версию docs
- Claude использует её для правильной реализации

**Когда полезно:**
- GSAP анимации (для Smart Widget)
- Framer Motion (для landing)
- Новые библиотеки (если добавляем что-то неизвестное)

**Пример:**
```
Никита: "Сделай сложную анимацию на GSAP для виджета"
Claude: [Context7] Скачай последнюю документацию GSAP
Claude: Использую GSAP ScrollTrigger + Timeline API (актуальная версия 3.12)
```

### Как ты будешь работать с этим:

**Явный запрос (когда нужна актуальная документация):**
```
"Проверь актуальную документацию GSAP через Context7"
"Какие новые фичи в Framer Motion 11?"
```

**Автоматически (когда Claude не уверен в API):**
Claude сам запросит Context7 если увидит что библиотека могла обновиться.

---

## 🚀 Как активировать MCP серверы

### Шаг 1: Перезапусти Claude Code
```bash
# Закрой текущую сессию Claude Code
# Открой заново в проекте
cd C:\Project\Floqly2
claude-code
```

### Шаг 2: Проверь что MCP серверы загрузились

В начале сессии Claude должен сказать что-то вроде:
```
✅ MCP серверы загружены:
- memory (Memory Bank)
- sequential-thinking (Sequential Thinking)
- filesystem (FileSystem)
- context7 (Context7)
```

Если не видишь такого сообщения, проверь:
```bash
cat ~/AppData/Roaming/Claude/claude_desktop_config.json
```

### Шаг 3: Протестируй Memory Bank

Спроси Claude:
```
"Запомни: я предпочитаю краткие ответы без воды"
```

Потом закрой Claude Code, открой заново и спроси:
```
"Что ты помнишь обо мне?"
```

Если Claude ответит что помнит про краткие ответы → Memory Bank работает! ✅

---

## 📊 Экономия токенов (ожидаемая)

| MCP | Экономия токенов | Как |
|-----|------------------|-----|
| **Memory Bank** | ~5-10k на сессию | Меньше повторений контекста |
| **Sequential Thinking** | ~10-15k на сложную задачу | Меньше ошибок и переделок |
| **FileSystem** | ~3-5k на сессию | Меньше промежуточных команд |
| **Context7** | ~2-5k на новую библиотеку | Правильное использование API с первого раза |

**ИТОГО:** ~20-35k токенов экономии на сессию

Вместе с оптимизацией `.claudeignore` (~15-20k):
**ОБЩАЯ ЭКОНОМИЯ: ~35-55k токенов на сессию**

---

## ⚠️ Troubleshooting

### Проблема: MCP серверы не загружаются

**Решение 1:** Проверь что Node.js установлен
```bash
node --version  # должно быть v18+
npx --version
```

**Решение 2:** Проверь путь в claude_desktop_config.json
```bash
cat ~/AppData/Roaming/Claude/claude_desktop_config.json
```

Для FileSystem путь должен быть абсолютным:
```json
"C:\\Users\\User\\.claude-worktrees\\Floqly2\\modest-wozniak"
```

**Решение 3:** Очисти кэш Claude
```bash
rm -rf ~/.claude/cache/
```

Перезапусти Claude Code.

### Проблема: Memory Bank не запоминает

**Решение:** Проверь что папка памяти существует
```bash
ls ~/.claude/memory/
```

Если её нет → создай:
```bash
mkdir -p ~/.claude/memory/
```

---

## 🎯 Итого для тебя (Никита)

### Что тебе нужно сделать:

1. **Перезапустить Claude Code** (чтобы загрузились MCP серверы)
2. **Протестировать Memory Bank** (команда выше)
3. **НИЧЕГО БОЛЬШЕ!** Всё работает автоматически

### Что изменится в работе:

**ДО (без MCP):**
```
Никита: "Сделай кнопку"
Claude: "Какой стиль? Какие цвета?"
Никита: "Минимализм, как я уже говорил 100 раз..."
→ Каждый раз повторять одно и то же
```

**ПОСЛЕ (с Memory Bank):**
```
Никита: "Сделай кнопку"
Claude: "Сделал в стиле precision minimalism (помню твоё предпочтение)"
→ Экономия времени и нервов
```

**ДО (без Sequential Thinking):**
```
Claude: Начал делать фичу → забыл про тесты → баг → переделка
→ Лишние токены и время
```

**ПОСЛЕ (с Sequential Thinking):**
```
Claude: [План] 1. Код 2. Тесты 3. Документация 4. Коммит
→ Всё работает с первого раза
```

---

## 📝 Дополнительно

### Где посмотреть логи MCP серверов:

```bash
# Логи Memory Bank
cat ~/.claude/debug/memory.log

# Общие логи Claude Code
cat ~/.claude/debug/claude-code.log
```

### Как временно отключить MCP сервер:

Отредактируй `~/AppData/Roaming/Claude/claude_desktop_config.json`:
```json
{
  "mcpServers": {
    "memory": {
      "disabled": true,  // ← добавь эту строку
      "command": "npx",
      ...
    }
  }
}
```

---

## 🔗 Полезные ссылки

- [Memory Bank MCP](https://github.com/modelcontextprotocol/servers/tree/main/src/memory)
- [Sequential Thinking MCP](https://github.com/modelcontextprotocol/servers/tree/main/src/sequential-thinking)
- [FileSystem MCP](https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem)
- [Context7 MCP](https://github.com/cyanheads/context7-mcp)

---

**Готово!** Теперь у Claude есть долгосрочная память, структурированное мышление и быстрый доступ к файлам. 🚀
