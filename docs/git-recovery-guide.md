# 🛡️ Git Recovery Guide — Восстановление проекта с GitHub

> **Когда использовать:** Если работал в Cursor/другой IDE и что-то сломалось локально.
> **Цель:** Восстановить проект к состоянию на GitHub одной командой.

---

## ⚡ ЭКСТРЕННОЕ ВОССТАНОВЛЕНИЕ (1 команда)

```bash
git fetch origin && git reset --hard origin/main && git clean -fd
```

**Что делает:**
- ✅ Скачивает последнюю версию с GitHub
- ✅ **УДАЛЯЕТ ВСЕ** локальные изменения
- ✅ Удаляет все новые файлы (которые могли быть созданы)
- ✅ Восстанавливает **ТОЧНО** ту версию что на GitHub

**⚠️ ВАЖНО:** Эта команда **БЕЗВОЗВРАТНО УДАЛИТ** все незакоммиченные изменения!

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### ДО работы в Cursor (создание точки восстановления):

```bash
# 1. Убедись что всё сохранено на GitHub:
git status
# Должно быть: "nothing to commit, working tree clean"

# 2. Если есть изменения — закоммить:
git add .
git commit -m "Сохранение перед работой в Cursor"
git push origin main

# 3. Создай тег-метку (точку восстановления):
git tag backup-before-cursor-$(date +%Y%m%d-%H%M%S)
git push origin --tags

# Готово! Теперь есть backup на GitHub
```

---

### ПОСЛЕ работы в Cursor (если что-то сломалось):

#### Вариант 1: Посмотреть ЧТО изменилось

```bash
# Посмотри список изменённых файлов:
git status

# Посмотри конкретные изменения:
git diff

# Посмотри изменения в конкретном файле:
git diff apps/web/src/app/globals.css
```

#### Вариант 2: Восстановить ОДИН файл

```bash
# Восстанови конкретный файл с GitHub:
git checkout origin/main -- apps/web/src/app/globals.css

# Проверь что восстановилось:
git status
```

#### Вариант 3: Восстановить ВСЁ (УДАЛИТ локальные изменения!)

```bash
# Скачай версию с GitHub:
git fetch origin

# Откати ВСЁ к версии с GitHub:
git reset --hard origin/main

# Удали новые файлы (которые Cursor создал):
git clean -fd

# Проверь что всё чисто:
git status
```

#### Вариант 4: Сохранить изменения Cursor в отдельную ветку

```bash
# 1. Создай ветку с изменениями Cursor:
git checkout -b cursor-changes-$(date +%Y%m%d)

# 2. Закоммить всё что Cursor сделал:
git add .
git commit -m "Изменения от Cursor (требует проверки)"

# 3. Вернись на чистую версию main:
git checkout main
git reset --hard origin/main

# Теперь:
# - main = чистая версия с GitHub ✅
# - cursor-changes-YYYYMMDD = ветка с изменениями Cursor (можно посмотреть потом)
```

---

## 🚨 ПАНИКА-КОМАНДЫ

### Команда №1: "Верни всё как было на GitHub"
```bash
git fetch origin && git reset --hard origin/main
```

### Команда №2: "Верни всё + удали новые файлы"
```bash
git fetch origin && git reset --hard origin/main && git clean -fd
```

### Команда №3: "Я случайно удалил коммиты, верни их!"
```bash
git reflog
# Найди строку с нужным коммитом:
# 6b641ca HEAD@{0}: commit: docs: update TODO...

git reset --hard HEAD@{0}
```

### Команда №4: "Я сломал main ветку, восстанови с backup"
```bash
# Посмотри список backup тегов:
git tag

# Восстанови с тега:
git reset --hard backup-before-cursor-20260208-143022
```

---

## 🎯 РЕКОМЕНДУЕМЫЙ WORKFLOW

### 1️⃣ Перед работой в Cursor:
```bash
git status && git push origin main
git tag backup-$(date +%Y%m%d-%H%M%S)
git push origin --tags
```

### 2️⃣ Работа в Cursor...
- Cursor меняет код
- Что-то идёт не так 😰

### 3️⃣ Если сломалось — восстановление:
```bash
git fetch origin && git reset --hard origin/main && git clean -fd
```

### 4️⃣ Если всё ок — сохранение:
```bash
git status
git diff
git add .
git commit -m "Изменения от Cursor"
git push origin main
```

---

## 📌 ПОЛЕЗНЫЕ КОМАНДЫ

### Посмотреть историю коммитов:
```bash
git log --oneline --graph --decorate -20
```

### Посмотреть все backup теги:
```bash
git tag | grep backup
```

### Удалить локальный backup тег:
```bash
git tag -d backup-before-cursor-20260208
```

### Удалить backup тег с GitHub:
```bash
git push origin :refs/tags/backup-before-cursor-20260208
```

### Сравнить локальную версию с GitHub:
```bash
git diff origin/main
```

### Скачать изменения с GitHub (БЕЗ применения):
```bash
git fetch origin
```

### Применить изменения с GitHub:
```bash
git pull origin main
```

---

## ⚠️ ЧТО НУЖНО ЗНАТЬ

### ❌ Что УДАЛИТ `git reset --hard`:
- Все незакоммиченные изменения в файлах
- Все staged изменения (добавленные через `git add`)
- Откатит файлы к указанному коммиту

### ❌ Что УДАЛИТ `git clean -fd`:
- Все новые файлы (которые не в Git)
- Все новые папки (которые не в Git)

### ✅ Что НЕ УДАЛИТСЯ:
- Файлы в `.gitignore` (node_modules, .env, и т.д.)
- Закоммиченная история (она в Git навсегда, даже после reset)
- Файлы на GitHub (они в безопасности)

---

## 🆘 ЭКСТРЕННАЯ ПОМОЩЬ

### "Я всё сломал, помогите!"
```bash
git reflog
git reset --hard HEAD@{0}
```

### "Я случайно удалил важный коммит!"
```bash
git reflog
# Найди хеш коммита и восстанови:
git cherry-pick <хеш>
```

### "Я сделал git reset --hard и потерял работу!"
```bash
git reflog
# Найди коммит ДО reset:
git reset --hard <хеш>
```

### "Локально всё сломано, просто верни с GitHub"
```bash
git fetch origin && git reset --hard origin/main && git clean -fd
```

---

## ✅ ЧЕКЛИСТ БЕЗОПАСНОСТИ

**Перед любым опасным действием:**
- [ ] `git status` — проверил что нет важных незакоммиченных изменений
- [ ] `git push origin main` — всё важное на GitHub
- [ ] `git tag backup-safe` — создал точку восстановления
- [ ] `git push origin backup-safe` — backup на GitHub

**После опасного действия:**
- [ ] `git status` — проверил что всё чисто
- [ ] `git log --oneline -5` — проверил историю коммитов
- [ ] Протестировал что проект работает (`pnpm dev`)

---

## 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [Официальная документация Git](https://git-scm.com/doc)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
- [Oh Shit, Git!?!](https://ohshitgit.com/) — как исправить ошибки Git

---

**Последнее обновление:** 2026-02-08
**Версия:** 1.0
**Автор:** Floqly Team
