================================================================================
  НАВИГАЦИЯ ПО ПРОЕКТУ FLOQLY — Подробная карта кодовой базы
================================================================================

Автор: Claude Sonnet 4.5
Дата создания: 2026-02-07
Назначение: Быстрая навигация по проекту для AI-ассистентов и разработчиков

📌 ВАЖНО: Этот файл создан для быстрого понимания где что находится.
           Используй его как справочник при работе с проектом.

================================================================================
  ОБЩАЯ СТРУКТУРА ПРОЕКТА
================================================================================

Floqly/
├── apps/                    # Приложения (Next.js, Widget, Admin)
│   ├── web/                 # 🎯 Основное Next.js приложение
│   ├── widget/              # 📦 Embed-скрипт виджета
│   └── admin/               # 🔧 Админ-панель (не реализована)
│
├── packages/                # Shared пакеты для монорепо
│   ├── ui/                  # 🎨 UI компоненты (Button, Card, Modal, etc.)
│   ├── config/              # ⚙️ Конфиги (ESLint, TypeScript, Tailwind)
│   ├── database/            # 💾 Supabase клиент и типы
│   └── analytics/           # 📊 Трекинг событий
│
├── docs/                    # 📚 Документация проекта
│   ├── PROGRESS.md          # Актуальный прогресс разработки
│   ├── DESIGN_SYSTEM.md     # Дизайн-система (precision minimalism)
│   ├── SPECIFICATION.md     # Техническая спецификация
│   ├── prd.md               # Product Requirements Document
│   ├── plans/               # Планы новых фич (создаются по мере надобности)
│   └── archived/            # Архив старых документов и логов
│
├── design-references/       # 🎨 Референсы дизайна (картинки, mockups)
│
├── .claudeignore            # Исключения для Claude Code
├── .cursorignore            # Исключения для Cursor AI
├── CLAUDE.md                # 📋 Инструкции проекта для AI
└── README.md                # Описание для GitHub

================================================================================
  APPS/WEB — ГЛАВНОЕ NEXT.JS ПРИЛОЖЕНИЕ
================================================================================

Путь: apps/web/

Структура:
apps/web/
├── src/
│   ├── app/                 # Next.js App Router (маршруты)
│   │   ├── (marketing)/     # 🌐 Публичные страницы (лендинг, превью)
│   │   ├── (tools)/         # 🛠️ Бесплатные инструменты (Cookie Generator)
│   │   ├── (dashboard)/     # 🔐 Личный кабинет (требует авторизации)
│   │   ├── api/             # 🔌 API endpoints
│   │   ├── auth/            # 🔑 Авторизация (Supabase Auth)
│   │   ├── globals.css      # 🎨 Глобальные стили + Tailwind
│   │   ├── layout.tsx       # Корневой layout
│   │   └── providers.tsx    # Провайдеры (Theme, Auth)
│   │
│   ├── components/          # ⚛️ App-specific компоненты
│   │   ├── header.tsx       # Шапка сайта
│   │   ├── footer.tsx       # Подвал
│   │   ├── theme-toggle.tsx # Переключатель темы
│   │   └── ...
│   │
│   └── lib/                 # 📦 Утилиты и хуки
│       ├── hooks/           # React хуки
│       │   ├── use-site-screenshot.ts  # Скриншоты сайта (Microlink API)
│       │   └── use-theme.ts            # Управление темой
│       ├── utils.ts         # Общие утилиты (cn, formatDate, etc.)
│       └── supabase/        # Supabase клиенты
│           ├── client.ts    # Клиент для клиентских компонентов
│           └── server.ts    # Клиент для серверных компонентов
│
├── public/                  # Статические файлы
│   ├── images/
│   ├── icons/
│   └── ...
│
├── next.config.ts           # Конфигурация Next.js
├── tailwind.config.ts       # Конфигурация Tailwind CSS v4
├── tsconfig.json            # TypeScript конфиг
└── package.json             # Зависимости

================================================================================
  МАРШРУТИЗАЦИЯ (ROUTE GROUPS)
================================================================================

Next.js использует Route Groups для разделения логики:

┌──────────────────────────────────────────────────────────────────────────┐
│ (marketing) — Публичные маркетинговые страницы                           │
└──────────────────────────────────────────────────────────────────────────┘

Путь: apps/web/src/app/(marketing)/

Маршруты:
  / (page.tsx)                           → Главная страница (лендинг)
  /widget-preview/ (page.tsx)            → Превью виджетов (список)
  /widget-preview/design-1-liquid-glass  → Дизайн #1
  /widget-preview/design-2-neomorphism   → Дизайн #2
  /widget-preview/design-3-minimalism    → Дизайн #3
  /widget-preview/design-4-abstract      → Дизайн #4
  /widget-preview/design-5-modern        → Дизайн #5
  /widget-preview/sandbox/               → Sandbox для тестирования
  /widget-preview/sandbox-1-plasma       → Sandbox #1
  /widget-preview/sandbox-2              → Sandbox #2
  ...

Layout: apps/web/src/app/(marketing)/layout.tsx
  - Минимальный layout (без header/footer для чистых демо)

Назначение:
  - Демонстрация дизайнов виджетов
  - Тестирование интеграции
  - Лендинг (главная страница)

┌──────────────────────────────────────────────────────────────────────────┐
│ (tools) — Бесплатные инструменты (PLG стратегия)                         │
└──────────────────────────────────────────────────────────────────────────┘

Путь: apps/web/src/app/(tools)/

Маршруты:
  /tools/cookie-generator (page.tsx)     → Cookie Generator (публичный)
  /tools/simple-widget (page.tsx)        → Simple Widget (не реализован)

Layout: apps/web/src/app/(tools)/layout.tsx
  - Header с брендингом
  - Footer
  - Правая часть экрана свободна (резерв под Smart Widget)

Cookie Generator (детально):
  📂 apps/web/src/app/(tools)/tools/cookie-generator/

  Файлы:
    page.tsx                      → Серверный компонент (обёртка)
    cookie-generator-client.tsx   → 🎯 ГЛАВНЫЙ клиентский компонент (658 строк)
    types.ts                      → TypeScript типы
    templates.ts                  → Шаблоны текстов для баннеров
    document-generator.ts         → Генерация документа (RTF/DOCX)

  Компоненты (components/):
    company-form.tsx              → Step 1: Данные компании
    cookie-config-form.tsx        → Step 2: Настройка cookies
    text-template-form.tsx        → Step 3: Текст документа
    banner-settings-form.tsx      → Step 4: Дизайн баннера
    banner-preview.tsx            → Превью баннера (desktop/mobile)
    document-preview.tsx          → Step 5: Финальный документ
    wizard-progress-card.tsx      → Карточка прогресса в sidebar
    cookie-impact-card.tsx        → Карточка влияния cookies (Step 2 sidebar)
    index.ts                      → Экспорты всех компонентов

  Структура шагов:
    Step 1 (Company)   → Ввод данных компании (название, сайт, ИНН)
    Step 2 (Cookies)   → Выбор типов cookies (необходимые, аналитика, маркетинг)
    Step 3 (Text)      → Редактирование текста документа
    Step 4 (Design)    → Настройка дизайна баннера (цвета, позиция, кнопки)
    Step 5 (Document)  → Финальный документ + скачивание

  Типы (cookie-generator-client.tsx):
    ActiveTab          → 'company' | 'cookies' | 'text' | 'design' | 'document'
    CompanyData        → Данные компании
    CookieConfig       → Конфигурация cookies
    BannerSettings     → Настройки баннера
    TextTemplate       → Шаблон текста

┌──────────────────────────────────────────────────────────────────────────┐
│ (dashboard) — Личный кабинет (требует авторизации)                       │
└──────────────────────────────────────────────────────────────────────────┘

Путь: apps/web/src/app/(dashboard)/

Маршруты:
  /dashboard (page.tsx)                              → Главная страница ЛК
  /dashboard/company-data (page.tsx)                 → Данные компании
  /dashboard/sites (page.tsx)                        → Управление сайтами
  /dashboard/widget (page.tsx)                       → Настройка виджета
  /dashboard/dialogs (page.tsx)                      → История диалогов
  /dashboard/integrations (page.tsx)                 → Интеграции (CRM, etc.)
  /dashboard/tools (page.tsx)                        → Инструменты
  /dashboard/tools/cookie-generator (page.tsx)       → Cookie Generator (ЛК версия)
  /dashboard/tools/cookie-generator/edit (page.tsx)  → Редактирование конфигурации
  /dashboard/settings (page.tsx)                     → Настройки аккаунта
  /dashboard/settings/billing (page.tsx)             → Биллинг и платежи
  /dashboard/subscription (page.tsx)                 → Подписка

Layout: apps/web/src/app/(dashboard)/layout.tsx
  - Header с навигацией ЛК
  - Sidebar (опционально)
  - Footer

Особенность:
  - Cookie Generator в ЛК использует ТЕ ЖЕ компоненты из (tools)
  - Путь к компонентам: apps/web/src/app/(tools)/tools/cookie-generator/components/
  - Не дублировать код! Импортировать из публичной версии.

┌──────────────────────────────────────────────────────────────────────────┐
│ api/ — API Endpoints                                                     │
└──────────────────────────────────────────────────────────────────────────┘

Путь: apps/web/src/app/api/

Структура:
  health/
    route.ts           → GET /api/health (проверка здоровья сервера)

  widgets/
    route.ts           → GET /api/widgets, POST /api/widgets
    [id]/
      route.ts         → GET/PUT/DELETE /api/widgets/[id]

Паттерн API endpoint:
  import { NextRequest, NextResponse } from 'next/server'
  import { z } from 'zod'
  import { createClient } from '@/lib/supabase/server'

  const schema = z.object({ ... })

  export async function POST(request: NextRequest) {
    try {
      const supabase = createClient()

      // Auth проверка
      const { data: { user }, error } = await supabase.auth.getUser()
      if (error || !user) {
        return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
      }

      // Валидация
      const body = await request.json()
      const validated = schema.parse(body)

      // Логика...

      return NextResponse.json({ success: true, data: result })
    } catch (error) {
      if (error instanceof z.ZodError) {
        return NextResponse.json({ error: 'Validation error', details: error.errors }, { status: 400 })
      }
      console.error('API Error:', error)
      return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
    }
  }

┌──────────────────────────────────────────────────────────────────────────┐
│ auth/ — Авторизация (Supabase Auth)                                     │
└──────────────────────────────────────────────────────────────────────────┘

Путь: apps/web/src/app/auth/

Структура:
  callback/
    route.ts           → GET /auth/callback (OAuth callback от Supabase)

  components/
    auth-form.tsx      → Форма входа/регистрации (email/password)
    auth-modal.tsx     → Модальное окно авторизации (286 строк)

Провайдеры OAuth:
  - Yandex
  - VK
  - Google
  - Telegram (через Supabase)

================================================================================
  COMPONENTS — APP-SPECIFIC КОМПОНЕНТЫ
================================================================================

Путь: apps/web/src/components/

Эти компоненты специфичны для apps/web и НЕ находятся в packages/ui.

Компоненты:
  header.tsx           → Шапка сайта (навигация, logo, auth)
  footer.tsx           → Подвал сайта (ссылки, копирайт)
  theme-toggle.tsx     → Переключатель темы (светлая/тёмная)
  theme-provider.tsx   → Провайдер темы (Context API)

Когда создавать компонент в apps/web/src/components/:
  ✅ Компонент специфичен для этого приложения (Header, Footer)
  ✅ Использует app-specific логику (навигация, роутинг)
  ✅ Не будет переиспользоваться в других apps

Когда создавать в packages/ui/:
  ✅ Компонент универсальный (Button, Card, Modal)
  ✅ Может использоваться в widget, admin, или других apps
  ✅ Чистый UI без бизнес-логики

================================================================================
  LIB — УТИЛИТЫ И ХУКИ
================================================================================

Путь: apps/web/src/lib/

┌──────────────────────────────────────────────────────────────────────────┐
│ hooks/ — React хуки                                                      │
└──────────────────────────────────────────────────────────────────────────┘

Файлы:
  use-site-screenshot.ts   → Получение скриншота сайта через Microlink API
  use-theme.ts             → Управление темой (светлая/тёмная)

use-site-screenshot.ts (детально):
  Назначение: Получить скриншот любого сайта для preview

  Использование:
    import { useSiteScreenshot } from '@/lib/hooks/use-site-screenshot'

    const { screenshot, loading, error } = useSiteScreenshot(websiteUrl)

  Особенности:
    - Debounce 1200ms (не спамить API при вводе URL)
    - Request cancellation (отменяет старые запросы)
    - URL нормализация (добавляет https:// если нужно)
    - Microlink API параметры: viewport 1280x800, full screenshot

  Используется в:
    - Cookie Generator Step 1 (превью сайта пользователя)

┌──────────────────────────────────────────────────────────────────────────┐
│ supabase/ — Supabase клиенты                                             │
└──────────────────────────────────────────────────────────────────────────┘

Файлы:
  client.ts            → Supabase клиент для клиентских компонентов
  server.ts            → Supabase клиент для серверных компонентов/API

Паттерн использования:

  // В клиентских компонентах (Client Components)
  import { createClient } from '@/lib/supabase/client'

  'use client'
  export function MyComponent() {
    const supabase = createClient()
    // ...
  }

  // В серверных компонентах/API (Server Components/Routes)
  import { createClient } from '@/lib/supabase/server'

  export async function MyServerComponent() {
    const supabase = createClient()
    const { data: { user } } = await supabase.auth.getUser()
    // ...
  }

┌──────────────────────────────────────────────────────────────────────────┐
│ utils.ts — Утилиты                                                       │
└──────────────────────────────────────────────────────────────────────────┘

Экспортируемые функции:
  cn(...inputs)        → Объединение Tailwind классов (clsx + twMerge)
  formatDate(date)     → Форматирование дат
  ...

Использование:
  import { cn } from '@/lib/utils'

  <div className={cn(
    'base-class',
    condition && 'conditional-class',
    className
  )} />

================================================================================
  PACKAGES — SHARED ПАКЕТЫ
================================================================================

Путь: packages/

Монорепозиторий использует pnpm workspaces для shared пакетов.

┌──────────────────────────────────────────────────────────────────────────┐
│ packages/ui — UI Компоненты                                              │
└──────────────────────────────────────────────────────────────────────────┘

Путь: packages/ui/src/

Компоненты:
  accordion.tsx        → Аккордеон (Item, Trigger, Content)
  badge.tsx            → Бейдж (варианты: default, success, warning, error)
  button.tsx           → Кнопка (варианты: primary, secondary, ghost, outline)
  card.tsx             → Карточка (Header, Title, Description, Content, Footer)
  dropdown.tsx         → Выпадающее меню (Item, Separator, Label)
  input.tsx            → Поле ввода + Label + Textarea
  modal.tsx            → Модальное окно (Header, Title, Description, Footer, Close)
  switch.tsx           → Переключатель (вкл/выкл)
  tabs.tsx             → Табы (List, Trigger, Content)
  utils.ts             → Утилиты (cn функция)
  index.ts             → Экспорты всех компонентов

Паттерн компонента:
  import { forwardRef } from 'react'
  import { cn } from './utils'

  interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
    variant?: 'primary' | 'secondary' | 'ghost'
    size?: 'sm' | 'md' | 'lg'
  }

  export const Button = forwardRef<HTMLButtonElement, ButtonProps>(
    ({ className, variant = 'primary', size = 'md', ...props }, ref) => {
      return (
        <button
          ref={ref}
          className={cn(
            'base-classes',
            variants[variant],
            sizes[size],
            className
          )}
          {...props}
        />
      )
    }
  )
  Button.displayName = 'Button'

Использование в apps/web:
  import { Button, Card, Modal } from '@floqly/ui'

  <Button variant="primary">Click me</Button>

┌──────────────────────────────────────────────────────────────────────────┐
│ packages/database — Supabase клиент и типы                               │
└──────────────────────────────────────────────────────────────────────────┘

Путь: packages/database/src/

Файлы:
  client.ts            → Supabase клиент
  types.ts             → TypeScript типы для БД (автогенерация)
  index.ts             → Экспорты

Схема БД (Supabase):
  profiles             → Профили пользователей (extends auth.users)
  projects             → Проекты пользователей
  widgets              → Виджеты
  conversations        → Диалоги с посетителями
  messages             → Сообщения в диалогах
  analytics_events     → События аналитики
  leads                → Лиды из виджета

Использование:
  import { createClient } from '@floqly/database'
  import type { Profile, Widget } from '@floqly/database'

┌──────────────────────────────────────────────────────────────────────────┐
│ packages/analytics — Трекинг событий                                     │
└──────────────────────────────────────────────────────────────────────────┘

Путь: packages/analytics/src/

Файлы:
  tracker.ts           → Основной класс трекера
  types.ts             → Типы событий
  index.ts             → Экспорты

Использование:
  import { track } from '@floqly/analytics'

  track('cookie_generator_completed', {
    step: 5,
    company: companyName
  })

┌──────────────────────────────────────────────────────────────────────────┐
│ packages/config — Конфигурации                                           │
└──────────────────────────────────────────────────────────────────────────┘

Путь: packages/config/

Структура:
  eslint/
    base.js            → Базовая ESLint конфигурация
    nextjs.js          → ESLint для Next.js

  typescript/
    base.json          → Базовая TypeScript конфигурация
    nextjs.json        → TypeScript для Next.js
    react-library.json → TypeScript для React библиотек

  tailwind/
    tailwind.config.ts → Tailwind CSS конфигурация

  prettier.config.js   → Prettier конфигурация

Использование в apps/web:
  // tsconfig.json
  {
    "extends": "@floqly/config/typescript/nextjs.json"
  }

  // eslint.config.mjs
  import { nextjs } from '@floqly/config/eslint'

  export default nextjs

================================================================================
  APPS/WIDGET — EMBED-СКРИПТ ВИДЖЕТА
================================================================================

Путь: apps/widget/

Назначение: Отдельный виджет, который встраивается на сайты пользователей.

Структура:
apps/widget/
├── src/
│   ├── index.ts         → Точка входа
│   ├── widget.ts        → Основной класс виджета
│   └── styles.css       → Стили виджета
│
├── dist/                → Собранный bundle (после pnpm widget:build)
│   └── widget.js        → Минифицированный скрипт
│
├── vite.config.ts       → Vite конфигурация
├── tsconfig.json        → TypeScript конфигурация
└── package.json         → Зависимости

Требования:
  - Bundle size < 50kb gzipped (желательно, но не строго)
  - Shadow DOM изоляция (обязательно)
  - Работа в iframe (Tilda, Wix)
  - Нейтральные имена классов (fl-*, не widget-*)
  - defer/async загрузка

Билд:
  pnpm widget:build      → Собирает dist/widget.js

Использование на сайте:
  <script src="https://cdn.floqly.ru/widget.js" defer></script>
  <script>
    window.FloqlyWidget.init({
      projectId: 'xxx',
      position: 'bottom-right'
    })
  </script>

================================================================================
  СТИЛИ И ДИЗАЙН
================================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│ Tailwind CSS v4                                                          │
└──────────────────────────────────────────────────────────────────────────┘

Главный файл стилей: apps/web/src/app/globals.css

Структура globals.css:
  @import "tailwindcss";         → Tailwind CSS v4 импорт

  @theme {                       → Кастомные CSS переменные
    --color-*: oklch(...);       → OKLCH цвета (светлая/темная тема)
    --font-*: ...;               → Шрифты
    --spacing-*: ...;            → Кастомные отступы
  }

  /* Глобальные стили */
  * { ... }
  html, body { ... }

Цвета (OKLCH):
  --color-background             → Фон страницы
  --color-foreground             → Основной текст
  --color-primary                → Акцентный цвет
  --color-secondary              → Вторичный цвет
  --color-muted                  → Приглушённый цвет
  --color-border                 → Бордеры

  ВАЖНО: Используй OKLCH, не HEX/RGB!

Анимации:
  - transition-colors duration-150    → Стандартная анимация
  - НЕ используй backdrop-blur, complex animations (кроме landing)
  - НЕ используй entry animations (fade-in, slide-in)

┌──────────────────────────────────────────────────────────────────────────┐
│ Дизайн-система                                                           │
└──────────────────────────────────────────────────────────────────────────┘

Файл: docs/DESIGN_SYSTEM.md

Философия: Precision Minimalism (Linear, Vercel, Raycast)

Три инструмента:
  1. Тонкие бордеры (border, border-border)
  2. Белые поверхности (bg-background, bg-card)
  3. Типографская иерархия (text-[14px], font-medium)

Запрещено в инструментах:
  ❌ backdrop-blur
  ❌ inner-градиенты
  ❌ shadow-sm на статических элементах
  ❌ rounded-2xl (используй rounded-lg)
  ❌ Entry animations

Кнопки CTA:
  ✅ bg-foreground text-background
  ❌ bg-primary (НЕ используй!)

Типографика:
  - Точные px размеры: text-[13px], text-[14px], text-[16px]
  - НЕ используй text-sm, text-base, text-lg

Правая часть экрана:
  - Резерв под Smart Widget (PLG стратегия)
  - НЕ блокировать sticky элементами, sidebars

Landing (исключение):
  - Отдельный дизайн, РАЗРЕШЕНЫ:
    ✅ GSAP анимации
    ✅ Framer Motion
    ✅ Scroll-triggered эффекты
    ✅ Creative layouts

================================================================================
  ПАТТЕРНЫ РАЗРАБОТКИ
================================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│ Создание нового UI компонента                                           │
└──────────────────────────────────────────────────────────────────────────┘

Вопрос 1: Где создавать?
  → packages/ui (shared)
    ✅ Универсальный компонент (Button, Card, Modal)
    ✅ Может использоваться в apps/web, apps/widget, apps/admin
    ✅ Нет бизнес-логики, чистый UI

  → apps/web/src/components (app-specific)
    ✅ Специфичен для apps/web (Header, Footer)
    ✅ Использует app-specific логику (роутинг, навигация)
    ✅ Не будет переиспользоваться

Шаблон (packages/ui):
  1. Создай файл: packages/ui/src/my-component.tsx
  2. Используй forwardRef + TypeScript
  3. Добавь variants через Tailwind
  4. Экспортируй в packages/ui/src/index.ts
  5. Используй в apps/web:
     import { MyComponent } from '@floqly/ui'

Пример:
  // packages/ui/src/badge.tsx
  import { forwardRef } from 'react'
  import { cn } from './utils'

  const variants = {
    default: 'bg-primary text-primary-foreground',
    success: 'bg-green-500 text-white',
    warning: 'bg-yellow-500 text-white',
  }

  interface BadgeProps extends React.HTMLAttributes<HTMLDivElement> {
    variant?: keyof typeof variants
  }

  export const Badge = forwardRef<HTMLDivElement, BadgeProps>(
    ({ className, variant = 'default', ...props }, ref) => {
      return (
        <div
          ref={ref}
          className={cn(
            'inline-flex items-center px-2 py-1 rounded-md text-xs font-medium',
            variants[variant],
            className
          )}
          {...props}
        />
      )
    }
  )
  Badge.displayName = 'Badge'

┌──────────────────────────────────────────────────────────────────────────┐
│ Создание нового API endpoint                                            │
└──────────────────────────────────────────────────────────────────────────┘

Шаги:
  1. Создай файл в apps/web/src/app/api/[route]/route.ts
  2. Используй zod для валидации
  3. Добавь auth проверку (Supabase)
  4. Обработка ошибок в try/catch
  5. Логирование (console.error)

Шаблон:
  // apps/web/src/app/api/my-endpoint/route.ts
  import { NextRequest, NextResponse } from 'next/server'
  import { z } from 'zod'
  import { createClient } from '@/lib/supabase/server'

  const requestSchema = z.object({
    name: z.string().min(1),
    email: z.string().email(),
  })

  export async function POST(request: NextRequest) {
    try {
      const supabase = createClient()

      // Auth проверка
      const { data: { user }, error: authError } = await supabase.auth.getUser()
      if (authError || !user) {
        return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
      }

      // Валидация
      const body = await request.json()
      const validated = requestSchema.parse(body)

      // Логика...
      const result = await doSomething(validated)

      return NextResponse.json({ success: true, data: result })
    } catch (error) {
      if (error instanceof z.ZodError) {
        return NextResponse.json(
          { error: 'Validation error', details: error.errors },
          { status: 400 }
        )
      }
      console.error('API Error:', error)
      return NextResponse.json(
        { error: 'Internal server error' },
        { status: 500 }
      )
    }
  }

┌──────────────────────────────────────────────────────────────────────────┐
│ Добавление нового шага в Cookie Generator                               │
└──────────────────────────────────────────────────────────────────────────┘

Шаги:
  1. Создай компонент в:
     apps/web/src/app/(tools)/tools/cookie-generator/components/

  2. Обнови тип ActiveTab:
     // types.ts
     export type ActiveTab =
       | 'company'
       | 'cookies'
       | 'text'
       | 'design'
       | 'document'
       | 'my-new-step'  // ← добавь сюда

  3. Добавь в cookie-generator-client.tsx:
     - Импорт компонента
     - Кнопку в wizard navigation
     - Рендер в switch/case

  4. Экспортируй из components/index.ts

Пример:
  // components/my-step-form.tsx
  'use client'

  interface MyStepFormProps {
    onNext: () => void
    onBack: () => void
  }

  export function MyStepForm({ onNext, onBack }: MyStepFormProps) {
    return (
      <div className="space-y-6">
        <h2>My Step</h2>
        {/* Форма... */}
        <div className="flex gap-4">
          <button onClick={onBack}>Назад</button>
          <button onClick={onNext}>Далее</button>
        </div>
      </div>
    )
  }

┌──────────────────────────────────────────────────────────────────────────┐
│ Работа с темами (светлая/тёмная)                                        │
└──────────────────────────────────────────────────────────────────────────┘

Хук: apps/web/src/lib/hooks/use-theme.ts

Использование:
  import { useTheme } from '@/lib/hooks/use-theme'

  export function MyComponent() {
    const { theme, setTheme } = useTheme()

    return (
      <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>
        Toggle theme
      </button>
    )
  }

CSS переменные (автоматически меняются):
  - Светлая тема: :root
  - Тёмная тема: .dark

Использование в Tailwind:
  <div className="bg-background text-foreground">
    Адаптивно под тему
  </div>

  <div className="dark:bg-slate-900">
    Специфично для тёмной темы
  </div>

================================================================================
  GIT WORKFLOW
================================================================================

Ветки:
  main               → Основная ветка (защищена)
  feature/*          → Новые фичи
  fix/*              → Багфиксы

Коммиты (Conventional Commits):
  feat: добавлена новая фича
  fix: исправлен баг
  docs: обновлена документация
  chore: техническая задача (настройка, оптимизация)
  refactor: рефакторинг без изменения функционала
  style: форматирование кода
  test: добавлены тесты

  Формат:
    type(scope): subject

    body

    Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>

  Примеры:
    feat(cookie-generator): add Step 6 for PDF export
    fix(auth): resolve OAuth callback redirect issue
    docs: update PROGRESS.md with completed tasks

Команды:
  git status                    → Проверить изменения
  git add .                     → Добавить все изменения
  git add path/to/file          → Добавить конкретный файл
  git commit -m "message"       → Создать коммит
  git push                      → Отправить на GitHub
  git log -1 --oneline          → Последний коммит

================================================================================
  ЧАСТО ИСПОЛЬЗУЕМЫЕ КОМАНДЫ
================================================================================

Разработка:
  pnpm dev                      → Запуск dev-сервера (http://localhost:3000)
  pnpm build                    → Production билд
  pnpm lint                     → Линтинг
  pnpm widget:build             → Билд виджета

Git:
  git status                    → Статус изменений
  git log -1 --oneline          → Последний коммит
  git add . && git commit && git push  → Быстрый коммит и пуш

Навигация:
  cd apps/web                   → Перейти в web приложение
  cd packages/ui                → Перейти в UI пакет
  ls -la                        → Показать все файлы
  find . -name "*.tsx"          → Найти все TSX файлы

Поиск:
  grep -r "текст" .             → Найти текст в файлах
  find . -name "filename"       → Найти файл по имени

================================================================================
  АНТИПАТТЕРНЫ (НЕ ДЕЛАЙ ЭТО!)
================================================================================

❌ Дублирование компонентов
   Плохо: Копировать код компонента из (tools) в (dashboard)
   Хорошо: Импортировать компонент из публичной версии

❌ Использование Framer Motion в apps/web
   Плохо: import { motion } from 'framer-motion'
   Хорошо: CSS анимации через transition-colors

❌ Хардкод цветов
   Плохо: className="bg-blue-500"
   Хорошо: className="bg-primary"

❌ Игнорирование DESIGN_SYSTEM.md
   Плохо: rounded-2xl backdrop-blur shadow-lg
   Хорошо: rounded-lg border transition-colors

❌ Создание компонента без forwardRef
   Плохо: export function Button(props) { ... }
   Хорошо: export const Button = forwardRef(...)

❌ API без валидации
   Плохо: const body = await request.json() // и сразу использовать
   Хорошо: const validated = schema.parse(body)

❌ Блокировать правую часть экрана
   Плохо: Sidebar справа, sticky элементы справа
   Хорошо: Правая часть свободна (резерв под Smart Widget)

================================================================================
  БЫСТРЫЕ ССЫЛКИ
================================================================================

Критические файлы:
  - CLAUDE.md                                    → Инструкции проекта
  - docs/PROGRESS.md                             → Актуальный прогресс
  - docs/DESIGN_SYSTEM.md                        → Дизайн-система
  - apps/web/src/app/globals.css                 → Глобальные стили
  - apps/web/src/app/(tools)/tools/cookie-generator/
      cookie-generator-client.tsx                → Главный компонент (658 строк)

Shared пакеты:
  - packages/ui/src/index.ts                     → UI компоненты
  - packages/database/src/types.ts               → TypeScript типы БД
  - packages/config/                             → Конфиги (ESLint, TS, Tailwind)

Хуки:
  - apps/web/src/lib/hooks/use-site-screenshot.ts  → Скриншоты сайта
  - apps/web/src/lib/hooks/use-theme.ts            → Управление темой

Утилиты:
  - apps/web/src/lib/utils.ts                    → cn() и другие утилиты
  - packages/ui/src/utils.ts                     → cn() для UI пакета

================================================================================
  КОНЕЦ НАВИГАЦИИ
================================================================================

Версия: 1.0
Последнее обновление: 2026-02-07
Создано: Claude Sonnet 4.5

💡 Используй этот файл как справочник при работе с проектом!
