# 📝 docs/drafts — Временные документы

Эта папка для временных Markdown файлов и черновиков, которые используются для коммуникации с Claude.

## 📂 Назначение

- **Черновики документации** — промежуточные версии документов перед переносом в основную структуру
- **Файлы для Claude** — MD файлы с контекстом/инструкциями для текущей сессии
- **Заметки** — временные заметки, которые не нужно коммитить в Git

## 🔄 Workflow

1. Создай файл в этой папке: `docs/drafts/my-notes.md`
2. Скажи Claude: "Прочитай docs/drafts/my-notes.md"
3. Claude прочитает и запишет важное в память
4. После прочтения можешь удалить файл или оставить для истории

## 🚫 Что НЕ коммитится

Папка `docs/drafts/` **исключена** из Git (см. `.gitignore`), поэтому:
- Временные файлы не попадут в историю коммитов
- Можно свободно экспериментировать
- Легко очистить: `rm docs/drafts/*.md`

## 📌 Исключения

Если какой-то черновик нужно сохранить в Git — перемести его в соответствующую папку:
- Документация → `docs/`
- Спецификации → `docs/SPECIFICATION.md`
- Планы → `docs/plans/`

## 🧹 Очистка

Периодически очищай папку от устаревших черновиков:

```bash
# Удалить все временные файлы
rm docs/drafts/*.md

# Или выборочно
rm docs/drafts/old-notes.md
```

---

**Примечание:** Эта папка создана для оптимизации работы с Claude Code — временные файлы не засоряют корень проекта и не увеличивают расход токенов при сканировании.
