# 🚀 Деплой на TimeWeb App Platform

## 📋 Подготовка к деплою

### 1. Получить ключи Supabase

Зайдите в Supabase Dashboard:
https://supabase.com/dashboard/project/rhdvlmhcfdqbqjgmcgcc/settings/api

Скопируйте:
- **URL**: `https://rhdvlmhcfdqbqjgmcgcc.supabase.co`
- **anon/public key**: `eyJhbGc...` (начинается с eyJ)
- **service_role key**: `eyJhbGc...` (начинается с eyJ)

⚠️ **ВАЖНО:** service_role key — секретный, НИКОГДА не выкладывать публично!

---

## 🔧 Настройка TimeWeb App Platform

### 1. Создание приложения

1. Зайдите в TimeWeb Cloud → App Platform
2. Создайте новое приложение
3. Выберите источник: **GitHub**
4. Подключите репозиторий `floqly`
5. Выберите ветку: `main`

### 2. Параметры сервера

**Окружение:**
- Тип: **Dockerfile** (автоматически найдёт `/Dockerfile`)
- CPU: **1 x 3.3 ГГц**
- RAM: **2 ГБ**
- NVMe: **30 ГБ**

**Сеть:**
- Диапазон адресов: `192.168.0.0/24` ✅ (оставить по умолчанию)
- Приватный IP: `192.168.0.4` ✅ (оставить по умолчанию)

### 3. Переменные окружения (Environment Variables)

⚠️ **ВАЖНО:** В TimeWeb нужно добавить переменные В ДВУХ МЕСТАХ:
1. **Environment Variables** (для runtime)
2. **Build Arguments** (для сборки Docker образа)

**Минимальный набор (для первого деплоя):**

```bash
# ========================================
# Environment Variables (runtime)
# ========================================
NEXT_PUBLIC_SUPABASE_URL=https://rhdvlmhcfdqbqjgmcgcc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key-с-Supabase
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key-с-Supabase
NODE_ENV=production

# ========================================
# Build Arguments (для сборки Docker)
# ========================================
NEXT_PUBLIC_SUPABASE_URL=https://rhdvlmhcfdqbqjgmcgcc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key-с-Supabase
```

**Опциональные переменные (добавить ПОСЛЕ подключения домена):**

```bash
# Добавьте когда настроите домен floqly.ru:
NEXT_PUBLIC_SITE_URL=https://floqly.ru
```

### 4. Настройка домена (опционально, можно сделать ПОТОМ)

**Для первого деплоя:**
- TimeWeb даст временный URL: `your-app-name.timeweb.cloud`
- HTTPS уже будет работать (автоматический SSL от TimeWeb)
- Можете протестировать приложение без домена

**Когда будете готовы подключить floqly.ru:**
1. В TimeWeb App Platform → Настройки → Домены
2. Добавьте домен: `floqly.ru`
3. Настройте DNS записи (A запись на IP TimeWeb)
4. Подождите пропагации DNS (~24 часа)
5. **SSL сертификат TimeWeb выпустит АВТОМАТИЧЕСКИ** (Let's Encrypt)
6. Добавьте в Environment Variables: `NEXT_PUBLIC_SITE_URL=https://floqly.ru`

---

## 🔄 Автодеплой из GitHub

TimeWeb автоматически деплоит при каждом пуше в `main`:

```bash
git add .
git commit -m "feat: new feature"
git push origin main
```

TimeWeb автоматически:
1. ✅ Подтягивает изменения из GitHub
2. ✅ Собирает Docker образ (через Dockerfile)
3. ✅ Запускает новый контейнер
4. ✅ Переключает трафик на новую версию

---

## ✅ Проверка деплоя

После успешного деплоя проверьте:

1. **Сайт доступен:**
   - https://floqly.ru
   - Должна открыться главная страница

2. **Supabase подключён:**
   - Откройте Chrome DevTools → Console
   - Не должно быть ошибок про Supabase

3. **Cookie Generator работает:**
   - Перейдите на https://floqly.ru/tools/cookie-generator
   - Заполните форму
   - Проверьте генерацию документа

---

## 🐛 Troubleshooting

### Проблема: Build failed (ошибка сборки)

**Решение:**
1. Проверьте логи сборки в TimeWeb Dashboard
2. Убедитесь что Build Arguments установлены (NEXT_PUBLIC_* переменные)
3. Проверьте что в Dockerfile нет ошибок

### Проблема: Container crashed (контейнер падает)

**Решение:**
1. Проверьте логи контейнера в TimeWeb Dashboard
2. Убедитесь что все env переменные установлены
3. Проверьте что SUPABASE_SERVICE_ROLE_KEY валидный

### Проблема: 502 Bad Gateway

**Решение:**
1. Контейнер может ещё запускаться (подождите 2-3 минуты)
2. Проверьте что порт 3000 открыт в контейнере
3. Проверьте логи контейнера

---

## 📊 Мониторинг

**TimeWeb Dashboard:**
- CPU usage (должно быть < 80%)
- Memory usage (должно быть < 1.8 GB из 2 GB)
- Logs (проверяйте на ошибки)

**Рекомендации:**
- Если CPU > 80% постоянно → увеличить до 2 CPU
- Если Memory > 1.8 GB → увеличить до 4 GB RAM

---

## 🔐 Безопасность

✅ **Что уже сделано:**
- service_role key хранится в env переменных (не в коде)
- Docker образ собирается на TimeWeb (не локально)
- HTTPS автоматически (через TimeWeb)

⚠️ **Что НЕ делать:**
- Не коммитить `.env` файл в Git
- Не публиковать service_role key нигде
- Не отключать HTTPS

---

## 📚 Полезные ссылки

- **TimeWeb Dashboard:** https://timeweb.cloud/
- **Supabase Dashboard:** https://supabase.com/dashboard/project/rhdvlmhcfdqbqjgmcgcc
- **GitHub Repository:** https://github.com/floqlyproject-hue/floqly
- **Documentation:** https://timeweb.cloud/docs/app-platform
