# Floqly

## О проекте

Floqly — экосистема виджетов для сайтов с AI-чатом. PLG-модель: бесплатные инструменты (Cookie Generator, Simple Widget) привлекают трафик, конвертируем в платный Умный виджет.

**Владелец:** Никита
**Партнёр:** Илья (Python backend для AI)
**Домен:** floqly.ru

---

## 📦 Продуктовая линейка

> **От бесплатных инструментов к платному AI-агенту**

| Инструмент | Тип | Стоимость | Описание | Функции |
|------------|-----|-----------|----------|---------|
| **Cookie Generator** | Utility | 🆓 FREE | Генератор политики cookie для сайта | Генерация документа, превью сайта, экспорт PDF |
| **Simple Widget** | Widget | 🆓 FREE | Базовая кнопка-виджет (реактивная, ждёт действий пользователя) | Раскрывающаяся панель с соц.сетями, мессенджерами и формой связи с оператором. Fixed на сайте клиента. |
| **Smart Widget** | AI Agent | 💰 PAID | **Умный виджет** — проактивный AI-агент (сам инициирует диалог) | Тот же визуальный дизайн что и Simple Widget, но с AI-интеллектом: анализ поведения, проактивные сообщения, знание контекста компании, анимации появления сообщений. |

**Ключевое отличие Simple vs Smart:**
- **Simple Widget** — статичный (пользователь сам открывает и выбирает канал связи)
- **Smart Widget** — динамичный (AI сам "заговаривает" с посетителем в нужный момент, используя поведенческие триггеры)

**PLG воронка:** Cookie Generator (FREE) или Simple Widget (FREE) или другие бесплатные инструменты → демо Smart Widget на сайте → конверсия в PAID подписку

---
## Бизнес-контекст Floqly

### 1. Целевая аудитория (ICP)
Мы ориентируемся на **Малый и Средний Бизнес и E-commerce в России**, у которых есть трафик, но низкая конверсия в продажу/заявку.(или недостаточно высокая по их мнению)
* **Владельцы бизнеса / Маркетологи:** Им не нужен "еще один чат", им нужен "автоматический продавец", который работает 24/7.
* **Сфера:** Интернет-магазины, услуги (недвижимость, юристы, образование), где цена потери клиента высока.
* **Боль:** У них нет бюджета на круглосуточный отдел продаж, а обычные чат-боты раздражают клиентов и не продают.

### 2. Главная проблема (The "Why")
**Проблема:** "Немые сайты". 98% посетителей уходят с сайта молча, потому что не нашли информацию, запутались или отвлеклись. Традиционные виджеты *реактивны* (ждут, пока им напишут) — это поздно.
**Решение:** Floqly делает сайт *проактивным*. Мы решаем проблему потери "теплых" лидов, перехватывая их внимание в момент сомнения (Exit Intent, долгое изучение цены, странные движения мышкой и так далее).

### 3. Smart-конверсия (Free → Paid)
Модель монетизации нашего сервиса строится на переходе от **Инструментов (Utility)** к **Ценности (Value)**:

* **Free Tier (Крючок):** Мы даем *обязательные* скучные инструменты бесплатно (Генератор Политики, Cookie, простой виджет-кнопка). Это решает юридическую/базовую боль и заставляет зарегистрироваться в ЛК.
* **The Gap (Разрыв):** Бесплатные инструменты просто "висят" на сайте. Они пассивны.
* **Paid Value (Умный виджет):** Пользователь переходит на платный тариф, потому что **Floqly начинает приносить деньги**.
    * *Сценарий:* В личном кабинете пользователь видит демо (или берет триал), где ИИ сам инициировал диалог и закрыл сделку, пока владелец спал.
    * Мы продаем не "чат", а "цифрового сотрудника". Оплата подписки дешевле зарплаты менеджера.
* Также помимо этого пользователь, оказавшись на нашем сайте в итоговой версии (например прийдя за генерацией политики cookie) - уже на этом этапе на нашем сайте будут демонстрироваться возможности нашего умного виджета, так как он будет встроен на сайт floqly.ru  
* Но также будут и другие способы привличения клиентов - например через главную страницу - она должна полностью раскрывать возможности и рассказывать про наш умный виджет

### 4. Что такое "Умный виджет" (Product Definition)
**Floqly Smart Widget** — это автономный AI-агент, интегрированный в интерфейс сайта, который обладает ключевыми отличиями от обычных чат-ботов:

1.  **Контекстная осведомленность:** Он знает, *откуда* пришел клиент (поисковый запрос в Яндекс/Google) и *на какой* странице находится. Он не спрашивает "Чем помочь?", а сразу предлагает решение по контексту (пример: "Ищете кроссовки для бега? У нас скидка на эту категорию").
2.  **Поведенческие триггеры:** Он анализирует движения курсора и скролл. Если клиент "мешкается" или хочет уйти — виджет вмешивается первым. Также он понимает на какой странице умный виджет на сайте сейчас находиться и может понимать контекст.
3.  **Хамелеон-дизайн:** Он визуально адаптируется под бренд (меняет форму/цвет), становясь нативной частью UI, а не чужеродным элементом - но это в идеале, по факту в конструкторе будет всё настраивать в личном кабинете дизайн умного виджета для своего сайта
4.  **Знание компании:** Важно! Умный виджет знает о компании клиента больше чем сам владелец бизнеса, и таким образом он может как и отвечать 24/7 на любые вопросы посетителей на сайте, так и вести диалог и закрывать с ними сделки

---

## 🚀 PLG-стратегия: Бесплатные инструменты как Lead Magnet

> 📖 **Полная стратегия:** [`docs/PLG_STRATEGY.md`](docs/PLG_STRATEGY.md)

### Ключевая концепция:

**Бесплатные инструменты (Cookie Generator и другие) — это не просто утилиты, а живая демонстрация силы Smart Widget через проактивное поведение.**

### Как это работает:

1. **Парсер сайта клиента** (фоновый)
   - При вводе URL сканируем HTML код сайта
   - Ищем следы конкурентов: Jivo, Bitrix24, Carrot Quest, Яндекс.Метрика
   - Определяем наличие/отсутствие виджетов связи

2. **Проактивное вмешательство Smart Widget** (WOW-эффект)
   - На Шаге 2 виджет **сам раскрывается** и перехватывает инициативу
   - Сценарий А: "Я нашёл на твоём сайте Jivo — давай я сам дозаполню этот раздел?"
   - Сценарий Б: "Твой сайт 'молчит' — ты теряешь 30% заявок!"
   - Автозаполнение полей на основе данных парсера

3. **Психологические триггеры**
   - Реципрокность: дали бесплатный документ → клиент лоялен
   - Эффект эксперта: "бот узнал мой софт" → доверие к технологии
   - Автозаполнение: "я сам дозаполню за тебя" → максимальная ценность

4. **Правая часть экрана = зона демонстрации**
   - Пользователь работает с формой слева
   - Smart Widget справа "анализирует", потом "вмешивается"
   - Визуальное доказательство что он умный, а не болталка

### ⚠️ ВАЖНО: Это применимо ко ВСЕМ бесплатным инструментам!

**Общий паттерн для всех инструментов Floqly:**
- Бесплатный инструмент решает конкретную проблему (юридическую, техническую, UX)
- В процессе использования Smart Widget **демонстрирует свою силу** через:
  - Проактивные советы и рекомендации
  - Автоматизацию рутинных действий
  - Экспертные подсказки на основе анализа
- WOW-эффект конвертирует пользователя в платную подписку

**Правило:** Никогда не делать бесплатный инструмент "кастрированным" ради продажи. Он должен быть **реально полезным** — только тогда сработает реципрокность и доверие.

**Детали:** См. полную стратегию в [`docs/PLG_STRATEGY.md`](docs/PLG_STRATEGY.md) (сценарии, технические детали, метрики)

---

## Технологический стек

| Слой | Технологии |
|------|------------|
| Frontend | Next.js 16.1.6 (App Router), React 19.2, TypeScript, Tailwind CSS v4 |
| Анимации | CSS transitions (apps/web), Framer Motion (landing), GSAP/Three.js (future) |
| Backend | Node.js (Express/Fastify) |
| Database | Supabase (PostgreSQL, Auth, Storage, Realtime) |
| Widget | Vanilla TS, Shadow DOM, Vite |
| **Хостинг** | **TimeWeb App Platform** (Docker, автодеплой из GitHub) |
| CI/CD | GitHub Actions |

---

## Структура проекта

> 📖 **Детальная навигация:** [`docs/PROJECT_NAVIGATION.md`](docs/PROJECT_NAVIGATION.md)
> (где что находится, примеры, паттерны)

```
floqly/
├── apps/
│   ├── web/           # Next.js (лендинг + ЛК + генераторы)
│   │   └── src/
│   │       ├── app/
│   │       │   ├── (marketing)/     # Публичные страницы
│   │       │   ├── (tools)/         # 🎯 Cookie Generator, Simple Widget
│   │       │   ├── (dashboard)/     # Личный кабинет
│   │       │   ├── api/             # API endpoints
│   │       │   └── globals.css      # Глобальные стили + Tailwind
│   │       ├── components/          # App-specific компоненты
│   │       └── lib/                 # Хуки, утилиты, Supabase
│   │
│   ├── widget/        # 🎯 Embed-скрипты виджетов (Vite + Vanilla TS + Shadow DOM)
│   │   │
│   │   │              # 📐 Workflow разработки виджетов:
│   │   │              # 1. Создаём дизайн Simple Widget → тестируем в песочнице → утверждаем
│   │   │              # 2. Переносим утверждённый дизайн в Smart Widget
│   │   │              # 3. Добавляем AI-интеграцию (API Ильи) + поведенческую логику
│   │   │              # 4. Итог: Smart Widget = Simple Widget (UI) + AI (логика)
│   │   │
│   │   ├── simple/    # Simple Widget (FREE) — базовая кнопка-виджет
│   │   │              # Реактивный (пользователь сам открывает)
│   │   │              # Разрабатывается ПЕРВЫМ как дизайн-фундамент
│   │   │              # Песочница для отработки UI/UX перед переносом в Smart
│   │   │
│   │   └── smart/     # Smart Widget (PAID) — проактивный AI-агент
│   │                  # Использует ТОТ ЖЕ дизайн что Simple Widget
│   │                  # Отличие: AI-логика (триггеры, проактивные сообщения, анимации)
│   │
│   └── admin/         # Админ-панель (заглушка)
│
├── packages/
│   ├── ui/            # 🎨 Shared UI (Button, Card, Modal, etc.)
│   ├── config/        # Конфиги (ESLint, TypeScript, Tailwind)
│   ├── database/      # Supabase клиент, типы БД
│   └── analytics/     # Трекинг событий
│
├── docs/              # 📚 Документация
│   ├── PROGRESS.md              # Актуальный прогресс
│   ├── DESIGN_SYSTEM.md         # Дизайн-система
│   ├── PROJECT_NAVIGATION.md    # Подробная карта проекта
│   ├── plans/                   # Планы фич
│   └── archived/                # Архив
│
└── design-references/ # Референсы дизайна
```

### 📂 Критические пути (быстрая навигация):

**Cookie Generator:**
- Публичный: `apps/web/src/app/(tools)/tools/cookie-generator/`
- В ЛК: `apps/web/src/app/(dashboard)/dashboard/tools/cookie-generator/edit/`
- Компоненты: `.../(tools)/tools/cookie-generator/components/`
- Главный файл: `cookie-generator-client.tsx` (658 строк)

**UI компоненты:**
- Shared: `packages/ui/src/` (Button, Card, Modal, etc.)
- App-specific: `apps/web/src/components/` (Header, Footer)

**Стили:**
- Глобальные: `apps/web/src/app/globals.css`
- Дизайн-система: `docs/DESIGN_SYSTEM.md`

**Хуки:**
- Скриншоты сайта: `apps/web/src/lib/hooks/use-site-screenshot.ts`
- Тема: `apps/web/src/lib/hooks/use-theme.ts`

---

## Команды

```bash
pnpm dev          # Запуск dev-сервера
pnpm build        # Production билд
pnpm test         # Запуск тестов
pnpm lint         # Линтинг
pnpm widget:build # Билд виджета
pnpm db:migrate   # Миграции Supabase
```

---

## Важные правила разработки

### Общие
- Язык интерфейса: **только русский** (i18n не нужен)
- Темы: светлая + тёмная **с первого дня**
- Mobile: desktop-first, но адаптив обязателен
- Accessibility: базовый уровень (клавиатура, aria-labels)

### Виджет (apps/widget)
- **Bundle size:** < 50kb gzipped (но не обязательно, можно и больше по весу, главное чтобы не очень долго грузилось)
- **Изоляция:** Shadow DOM обязателен
- **Работа в iframe:** да (Tilda, Wix)
- Нейтральные имена (избежать AdBlock): `fl-helper.js`, не `ad-widget.js`
- Загрузка: `defer` или `async`

### Безопасность
- CORS только для разрешённых доменов
- JWT для API
- Rate limiting
- Не хранить sensitive data в localStorage

### Код
- TypeScript strict mode
- Prefer функциональные компоненты
- Tailwind для стилей (не CSS-in-JS)
- Тесты для критических путей (auth, API)

---

## ❌ Антипаттерны (НЕ делай это!)

> **Для Никиты:** Это список того, что НЕ нужно делать в проекте и почему.
> **Для Claude:** Если видишь что делаешь что-то из этого списка — остановись и переделай.

### 🎨 Дизайн и UI

**❌ НЕ используй Framer Motion в apps/web (инструменты и ЛК)**

**Что это значит:** Framer Motion — библиотека для сложных анимаций.

**Почему нельзя в инструментах/ЛК:**
- Увеличивает размер страницы (долгая загрузка)
- Противоречит дизайн-системе (precision minimalism)
- На инструментах нужна скорость, не анимации

**Где МОЖНО использовать:**
- ✅ Главная страница (landing)
- ✅ Виджеты (apps/widget) — Simple Widget, Smart Widget
- ✅ Pop-up cookie (на сайте клиента)

**Где НЕЛЬЗЯ:**
- ❌ Инструментальные страницы (Cookie Generator UI, формы)
- ❌ Личный кабинет (dashboard, конструкторы)
- ❌ Служебные страницы (контакты, блог, документация)

**Вместо этого (для инструментов/ЛК):** Простые CSS анимации (`transition-colors duration-150`)

---

**❌ НЕ используй backdrop-blur, rounded-2xl, inner-градиенты в инструментах**

**Что это значит:**
- `backdrop-blur` — размытый фон (эффект "стекла")
- `rounded-2xl` — очень скруглённые углы
- Inner-градиенты — градиенты внутри элементов

**Почему нельзя:**
- Не соответствует дизайн-системе (precision minimalism)
- Выглядит "тяжело", отвлекает от задачи
- Цель инструментов — простота и скорость

**Вместо этого:**
- `rounded-lg` (средние углы)
- Простые бордеры (`border`)
- Однотонные цвета

**Где можно:** Только на landing странице

---

**❌ НЕ размещай статичные элементы справа в инструментах**

**Что это значит:** Правая часть экрана должна визуально оставаться свободной, где это возможно.

**🚫 Что НЕЛЬЗЯ размещать справа:**
- Fixed sidebar (зафиксированная боковая панель)
- Sticky элементы (прилипающие при скролле)
- Постоянные баннеры, карточки, виджеты
- Статичные блоки с контентом

**✅ Что МОЖНО (временные элементы поверх контента):**
- Модальные окна (открываются по действию, потом закрываются)
- Tooltips и notifications (всплывают и исчезают)
- Dropdown меню (раскрываются по клику, потом сворачиваются)
- Toast уведомления (автоматически исчезают)


**Почему это важно:**
- **PLG стратегия:** Правая часть зарезервирована под **Smart Widget** (он будет fixed, поверх контента)
- Статичные элементы справа будут визуально конфликтовать с виджетом и отвлекать внимание
- Цель: пользователь работает с бесплатным инструментом → видит справа демо Smart Widget → конвертируется в платную подписку

**Правило:** Дизайн должен оставлять "воздух" справа, чтобы Smart Widget был главным фокусом внимания в этой зоне.


---

### 💻 Код и архитектура

**❌ НЕ дублируй код между (tools) и (dashboard)**

**Что это значит:** Не копируй компоненты из публичной версии Cookie Generator в версию ЛК.

**Почему нельзя:**
- Код поддерживается в двух местах (двойная работа при изменениях)
- Легко забыть обновить одну из версий
- Баги появляются в одной версии, в другой остаются

**Что делать:**
- Dashboard версия ИМПОРТИРУЕТ компоненты из публичной
- Путь: `apps/web/src/app/(tools)/tools/cookie-generator/components/`
- Одна версия компонента = одна правда

**Пример:**
```tsx
// ❌ ПЛОХО: копировать компонент
// apps/web/src/app/(dashboard)/.../components/company-form.tsx
export function CompanyForm() { /* дублированный код */ }

// ✅ ХОРОШО: импортировать из публичной версии
import { CompanyForm } from '@/app/(tools)/tools/cookie-generator/components'
```

---

**❌ НЕ создавай компонент в packages/ui если он специфичен для apps/web**

**Что это значит:**
- `packages/ui` — для УНИВЕРСАЛЬНЫХ компонентов (Button, Card)
- `apps/web/components` — для СПЕЦИФИЧНЫХ (Header, Footer)

**Почему нельзя:**
- Header зависит от навигации apps/web (не универсален)
- Загрязняет packages/ui ненужными зависимостями
- Другие apps (widget, admin) не смогут использовать

**Когда создавать в packages/ui:**
- ✅ Компонент универсален (Button, Modal, Input)
- ✅ Нет привязки к apps/web (роутинг, навигация)
- ✅ Может использоваться в widget, admin

**Когда создавать в apps/web/components:**
- ✅ Специфичен для этого приложения (Header, Footer)
- ✅ Использует Next.js роутинг (`next/link`)
- ✅ Не будет переиспользоваться

---

**❌ НЕ хардкоди цвета (bg-blue-500, text-red-600)**

**Что это значит:** Не используй конкретные цвета из Tailwind палитры.

**Почему нельзя:**
- Не работает с тёмной темой (синий на тёмном фоне плохо виден)
- При смене дизайн-системы нужно менять везде вручную
- Нарушает единообразие

**Что делать:** Используй семантические цвета из globals.css
- ✅ `bg-background` (фон страницы)
- ✅ `bg-foreground` (основной текст)
- ✅ `bg-primary` (акцентный цвет)
- ✅ `bg-muted` (приглушённый)
- ✅ `border-border` (бордеры)

**Пример:**
```tsx
// ❌ ПЛОХО
<div className="bg-blue-500 text-white">

// ✅ ХОРОШО
<div className="bg-primary text-primary-foreground">
```

**Почему работает:** globals.css меняет цвета автоматически при переключении темы.

---

**❌ НЕ используй i18n (перевод на другие языки)**

**Что это значит:** Не добавляй библиотеки для перевода текста (i18next, react-intl).

**Почему нельзя:**
- Проект только для русского рынка
- Увеличивает сложность кода
- Замедляет разработку (каждый текст в два раза дольше)

**Что делать:** Все тексты сразу на русском в компонентах.

---

### 🔐 Безопасность и API

**❌ НЕ создавай API endpoint без валидации (zod)**

**Что это значит:** Когда пользователь отправляет данные на сервер, нужно проверить что они корректны.

**Почему нельзя:**
- Пользователь может отправить что угодно (взлом, баги)
- Данные могут сломать приложение (например, текст вместо числа)

**Что делать:** Использовать библиотеку `zod` для проверки.

**Пример:**
```tsx
// ❌ ПЛОХО: нет проверки
const body = await request.json()
const name = body.name  // может быть undefined, число, объект — что угодно!

// ✅ ХОРОШО: с проверкой
const schema = z.object({
  name: z.string().min(1),  // обязательная строка, минимум 1 символ
  email: z.string().email() // обязательный email
})
const validated = schema.parse(body)  // выбросит ошибку если неверный формат
const name = validated.name  // гарантированно строка!
```

---

**❌ НЕ храни пароли, токены в localStorage**

**Что это значит:** `localStorage` — место в браузере где можно сохранить данные.

**Почему нельзя:**
- JavaScript на сайте может прочитать (XSS атака)
- Пароль украдут хакеры

**Что делать:**
- Пароли НЕ хранить НИГДЕ (только на сервере)
- Токены (ключи доступа) хранить в httpOnly cookies (безопасно)
- Использовать Supabase Auth (он всё делает правильно)

---

### 📦 Зависимости и производительность

**❌ НЕ используй CSS-in-JS библиотеки (styled-components, emotion)**

**Что это значит:** Библиотеки где CSS пишется внутри JavaScript кода.

**Почему нельзя:**
- Увеличивает размер страницы
- Замедляет загрузку
- В проекте уже есть Tailwind CSS (конфликт подходов)

**Что делать:** Используй только Tailwind CSS.

---

**❌ НЕ добавляй тяжёлые библиотеки без обсуждения**

**Что это значит:** Не устанавливай большие npm пакеты просто так.

**Почему нельзя:**
- Увеличивает размер приложения (медленная загрузка для пользователей)
- Может конфликтовать с существующим кодом
- Добавляет complexity (сложность)

**Что делать:**
- Сначала спроси: "Можно ли обойтись без библиотеки?"
- Проверь размер библиотеки (bundlephobia.com)
- Обсуди с Никитой если библиотека > 50kb

---

## 💡 Почему антипаттерны важны

**Для Никиты:**
Эти правила основаны на опыте тысяч проектов. Они помогают избежать:
- Медленной загрузки сайта (пользователи уходят)
- Багов и уязвимостей (взломы, потеря данных)
- Дублирования работы (копипаст кода)
- Несоответствия дизайну (хаос вместо единого стиля)

**Для Claude:**
Если собираешься делать что-то из списка выше — ОСТАНОВИСЬ и переделай правильно.
Лучше потратить 5 минут на правильное решение, чем потом исправлять баги неделю.

---

## 🎨 Дизайн — Правила и зоны применения

> ⚠️ **ВАЖНО:** Дизайн-правила зависят от **контекста** — что именно разрабатываем.

---

### 📋 **Где применяется Precision Minimalism (строгие правила):**

**✅ Применяется к:**
1. **Инструментальные страницы** (UI самих инструментов):
   - Cookie Generator — форма, шаги, кнопки
   - Другие генераторы и инструменты в будущем

2. **Личный кабинет** (dashboard):
   - Навигация, таблицы, формы настроек
   - Все страницы конструкторов виджетов (где пользователь настраивает виджет)

3. **Служебные страницы**:
   - Контакты, блог, документация, FAQ
   - Страницы авторизации/регистрации

**для Precision Minimalism обязательно следовать:** [`docs/DESIGN_SYSTEM.md`](docs/DESIGN_SYSTEM.md)

**Правила:**
- Стиль: precision minimalism (Linear, Vercel, Raycast)
- Три инструмента: тонкие бордеры, белые поверхности, типографская иерархия
- Никаких backdrop-blur, inner-градиентов, shadow на статике, rounded-2xl
- Кнопки CTA: `bg-foreground text-background`, не `bg-primary`
- Анимации: только micro-interactions (transition 150ms), без entry-анимаций
- Правая часть экрана свободна — зарезервирована под Умный Виджет

---

### 🚀 **Где НЕ применяется минимализм (креатив без ограничений):**

**❌ Правила НЕ действуют для:**

#### **1. Виджеты (apps/widget)**
- **Simple Widget** — креативный дизайн, любые техники
- **Smart Widget** — креативный дизайн, анимации появления сообщений
- **Подход:** Копирование CSS/JS/HTML кода, референсы, креативные решения
- **Технологии:** Можно всё — GSAP, custom animations, градиенты, shadows, blur

#### **2. Pop-up Cookie или другие бесплатные инструменты (то что у клиента на сайте)**
- **Дизайн:** Разнообразный — от минимализма до футуризма
- **Цель:** Адаптироваться под дизайн сайта клиента
- **Подход:** Шаблоны с разными стилями (минимализм, модерн, футуристик и т.д.)

#### **3. Главная страница (landing)**
- **Цель:** Презентация Smart Widget, Site of the Day на Awwwards
- **Технологии:** Framer Motion, GSAP, Three.js, scroll-triggered анимации, creative layouts
- **Подход:** По референсам, возможно MCP Figma (импорт дизайнов из Figma)
- **Вдохновение:** Locomotive, Cuberto, Unseen Studio
- **Референсы:** `/design-references/`

---

### 🧩 **Краткая шпаргалка для Claude:**

| Что разрабатываем | Минимализм? | Технологии | Пример |
|-------------------|-------------|------------|--------|
| Формы инструментов (Cookie Generator и другие страницы в будущем) | ✅ ДА | Tailwind, CSS transitions | Linear, Vercel |
| Личный кабинет | ✅ ДА | Tailwind, CSS transitions | Raycast, Vercel Dashboard |
| Служебные страницы | ✅ ДА | Tailwind, CSS transitions | Документация Linear |
| **Simple/Smart Widget** | ❌ НЕТ | Любые (GSAP, custom CSS, etc.) | Креативный подход |
| **Pop-up Cookie** | ❌ НЕТ | Любые (шаблоны разных стилей) | Адаптивный дизайн |
| **Landing (главная)** | ❌ НЕТ | Framer Motion, GSAP, Three.js | Awwwards, Cuberto |

---

### ✅ **Итого:**

**Минимализм = инструментальные UI + личный кабинет + служебные страницы**
**Креатив = виджеты + pop-up cookie + главная страница**

---

## Интеграции

### ✅ Работает сейчас:

**Инфраструктура:**
- **Supabase:** База данных (PostgreSQL), авторизация, хранилище файлов
  - ✅ Remote проект настроен: `rhdvlmhcfdqbqjgmcgcc`
  - ⚠️ **TODO:** Сгенерировать реальные типы БД (сейчас placeholder!)
  - 📖 Инструкции: `packages/database/README.md`
- **TimeWeb App Platform:** Production хостинг (РФ, автодеплой из GitHub)
  - ✅ Конфигурация: Dockerfile + Docker Compose
  - ✅ Параметры: 1 CPU x 3.3 ГГц, 2 GB RAM, 30 GB NVMe
  - ✅ Сеть: 192.168.0.0/24 (приватная подсеть Docker)
  - 📖 Env vars: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, NODE_ENV, NEXT_PUBLIC_SITE_URL
- **GitHub Actions:** CI/CD (автоматическое тестирование и деплой)

**Внешние API:**
- **Microlink API:** Скриншоты сайтов для превью в Cookie Generator

---

### 📋 В планах (по приоритету):

**High Priority:**
- **OAuth провайдеры:** Yandex, VK, Google, Telegram (для входа пользователей)
- **AI API (Илья):** Python backend для Smart Widget
  - **Формат:** REST API (предположительно)
  - **Функции:** Проактивные сообщения, анализ поведения пользователя, контекстные ответы
  - **Статус:** ⚠️ Требует уточнений (спецификация в разработке)
  - **Endpoint примеры:** TBD (уточнить у Ильи)

**Medium Priority:**
- **Telegram Bot:** Уведомления о новых лидах из виджета
- **CRM интеграции:** Webhook API для передачи лидов (AmoCRM, Битрикс24)
- **Yandex Метрика:** Аналитика посещений и конверсий

**Low Priority:**
- **Sentry:** Error tracking (мониторинг и логирование ошибок)
- **CDN (Timeweb):** Раздача виджета через cdn.floqly.ru

---

## Контакты и ресурсы

- **Спецификация:** `docs/SPECIFICATION.md`
- **PRD:** `docs/prd.md`
- **Прогресс:** `docs/PROGRESS.md`
- **План виджета:** `docs/WIDGET-CONSTRUCTOR-PLAN.md`
- **GitHub:** (настроить)
- **Supabase:** (настроить)

### 🛠️ Skills (Claude Code)

**Статус:** ✅ **Работают** (настроены вручную Никитой)

**Доступные skills в проекте:**
- `frontend-design` — дизайн-паттерны для фронтенда 
- `web-design-guidelines` — гайдлайны по веб-дизайну 
- `tailwind-design-system` — дизайн-система на Tailwind
- `gsap` — GSAP техники и паттерны 
- `threejs-animation` — 3D анимации на Three.js 
- `typescript-advanced-types` — продвинутые типы TypeScript
- `vercel-react-best-practices` — best practices для Next.js/React
- `better-auth-best-practices` — аутентификация и безопасность
- `supabase-postgres-best-practices` — Supabase и PostgreSQL паттерны
- `seo-audit` — SEO оптимизация
- `nextjs` — Next.js 16 паттерны и best practices
- `api`, `component`, `widget-check` — шаблоны для быстрого создания

**MCP серверы:**
- ✅ **Playwright** — автоматизация браузера (тестирование виджетов, скриншоты UI, дебаг)
- 📋 **Figma** (TODO) — импорт дизайнов из Figma (планируется для landing page)

---

## Правила для Claude Code

### 📖 В начале КАЖДОЙ сессии:
1. **Спроси** пользователя что нужно сделать в этой сессии
2. **Прочитай** `docs/PROGRESS.md` → секция "Активные задачи для Claude"
3. **Посмотри** TODO ниже (что в High Priority)
4. **Добавь** новую задачу от пользователя в High Priority (если дал устно)
5. **Начинай** работу

### 🔄 Во время работы:
**После КАЖДОЙ выполненной задачи** (не в конце сессии!):
1. Обнови `docs/PROGRESS.md` с выполненной задачей
2. Перемести задачу в "✅ Выполнено" в TODO ниже
3. Закоммить изменения
4. Спроси: "Продолжаем или на этом всё?"

**Почему по ходу, а не в конце?**
- На случай если лимиты токенов закончатся внезапно
- Контекст сохраняется в Git после каждой задачи
- Можно остановиться в любой момент без потери прогресса

### ✅ В конце сессии:
**Когда пользователь говорит** "конец сессии" / "на этом всё" / "завершаем":
1. Проверь что `docs/PROGRESS.md` актуален
2. Обнови секцию "Последняя сессия" с датой и итогами
3. Предложи план задач на следующую сессию
4. Финальный коммит (если есть незакоммиченные изменения)

### 💡 Работа с .claudeignore:
- `.claudeignore` исключает файлы из автоматического сканирования (Glob, Grep)
- Явные запросы типа `Read("node_modules/package.json")` работают всегда
- При добавлении новых build artifacts → добавляй в `.claudeignore`

### 🎨 Правило использования скиллов для дизайна:

**ОБЯЗАТЕЛЬНОЕ ПРАВИЛО:** При работе с Precision Minimalism дизайном (инструменты, ЛК, служебные страницы) **ВСЕГДА используй** один из этих скиллов:

1. **`frontend-design`** — для создания новых UI компонентов и страниц
   - Используй когда: создаёшь новый компонент, страницу, или дорабатываешь дизайн
   - Пример: "Создай карточку для Cookie Impact", "Доработай дизайн формы в Step 3"

2. **`web-design-guidelines`** — для ревью и аудита существующего дизайна
   - Используй когда: проверяешь готовый UI на соответствие гайдлайнам
   - Пример: "Проверь Step 2 на соответствие Precision Minimalism", "Аудит дизайна Cookie Generator"

**Почему это важно:**
- Скиллы содержат глубокие знания о Precision Minimalism (Linear, Vercel, Raycast)
- Они помогают избежать антипаттернов (backdrop-blur, shadow-sm, rounded-2xl)
- Гарантируют соответствие дизайн-системе


### 🎯 Управление TODO:
- **Пользователь** даёт задачу устно → **Claude** добавляет в High Priority
- **Пользователь** может сам обновить TODO перед сессией (в CLAUDE.md)
- **Claude** обновляет статус задач по ходу работы (перемещает в ✅ Выполнено)
- **Ротация выполненных:** держать только последние 3-5 задач в "✅ Выполнено"
  - Старые задачи переносить в `docs/PROGRESS.md` (полная история)
  - Это предотвращает рост CLAUDE.md и экономит токены

### 🚨 Чек-лист перед коммитом:

**Перед тем как сделать коммит, проверь:**

- [ ] **Прочитал файл перед редактированием** (использовал Read → Edit, а не Write для существующих файлов)
- [ ] **Семантические цвета** (использовал `bg-background`, `text-foreground` — НЕ `bg-blue-500`, `text-red-600`)
  - ⚠️ Только для инструментов/ЛК! Виджеты — любые цвета разрешены
- [ ] **Framer Motion** (НЕ добавлял в инструменты/ЛК — только для landing и виджетов)
  - ✅ Можно: landing, apps/widget, pop-up cookie
  - ❌ Нельзя: инструменты, личный кабинет, служебные страницы
- [ ] **Правая часть свободна** (нет fixed sidebar справа, нет статичных элементов)
  - ⚠️ Только для инструментов! Виджеты размещаются как нужно
- [ ] **Компоненты не дублируются** (Dashboard импортирует из public версии, а не копирует)
- [ ] **API с валидацией** (если создал endpoint — добавил zod-схему)
- [ ] **Обновил PROGRESS.md** (если завершил задачу)
- [ ] **Тесты пройдены** (если менял критический код)

**Если хоть один пункт НЕ выполнен — исправь перед коммитом!**

### 📋 Режим планирования (для новых фич):
**Когда использовать:**
- Пользователь просит спланировать новую фичу
- Задача сложная, требует архитектурных решений
- Нужно продумать подход перед началом работы

**Процесс:**
1. **Вход в план-режим:** используй EnterPlanMode
2. **Исследование:** изучи кодовую базу, найди связанные файлы
3. **Создание плана:** создай файл `docs/plans/PLAN-feature-name.md`
4. **Структура плана:**
   ```markdown
   # План: [Название фичи]

   ## Цель
   ## Технический подход
   ## Файлы для изменения/создания
   ## Зависимости (npm packages)
   ## Оценка сложности
   ## Риски
   ## Тестирование
   ```
5. **Одобрение:** покажи план пользователю, жди одобрения
6. **Работа по плану:** после одобрения начинай реализацию
7. **Обращение к плану:** во время работы можешь Read() файл плана для уточнений
8. **Обновление плана:** если что-то изменилось в процессе — обнови план

**Важно:**
- План создаётся ОДИН раз в начале работы над фичей
- План остаётся в `docs/plans/` для истории
- Можно обращаться к плану в процессе работы
- После завершения фичи → отметить в `docs/PROGRESS.md`

---

## TODO (текущие задачи)

> ⚠️ Обновляется каждую сессию! Claude, начинай отсюда.
>
> **Правило:** В секции "✅ Выполнено" держи только последние 3-5 задач.
> Остальные переноси в `docs/PROGRESS.md` → История изменений.

### 🔴 High Priority (в работе):
- [ ] **Доработать Step 2 Cookie Generator**
  - Убрать странные/лишние сервисы из разделов (аналитика, реклама)
  - Оставить только популярные и актуальные для российского рынка
- [ ] **Улучшить описание блока "Реклама и ретаргетинг" в Step 2**
  - Расписать что это не только ретаргетинг, но и показ рекламы на сайте
  - Упомянуть баннеры, рекламные блоки, партнерские программы
  - Сделать название и описание более понятным
- [ ] **Доработать Step 3 Cookie Generator**
  - Поработать с форматами вывода документа (PDF, HTML, TXT)
  - Добавить возможность редактирования документа перед экспортом
  - Улучшить превью документа
- [ ] **Протестировать Cookie Generator с новым шаблоном**
  - Проверить Step 1 (Company) → данные сохраняются
  - Проверить Step 2 (Cookie Policy) → все 4 блока работают, форма "Другое" добавляет сервисы
  - Проверить Step 3 (Document) → документ генерируется с правильными переменными
  - Проверить автовключение трансграничной передачи при выборе Google/Facebook Ads
  - Проверить что Step 4 (Design) и Step 5 (Result) работают как раньше
- [ ] **🔜 СКОРО: Справочник популярных виджетов для автозаполнения**
  - База данных: Jivo, Bitrix24, CarrotQuest, и т.д.
  - Поля: название, правообладатель, URL политики, сигнатуры для парсера
  - Файл: `apps/web/src/lib/data/widgets-database.ts`
- [ ] **🔜 СКОРО: Технические триггеры для умного виджета**
  - Событие `onExternalServicesClick` (клик на "Средства взаимодействия")
  - Состояние `parserData` (данные от парсера)
  - Триггер `onAutoFillComplete` (после автозаполнения)
  - Визуальная индикация парсера ("Анализирую скрипты...")
- [ ] **Реализовать парсер сайтов для Cookie Generator** (делаем сами, не Илья!)
  - Сканирование HTML кода сайта клиента (фоновое, при вводе URL)
  - Детекция сервисов: Jivo, Bitrix24, Carrot Quest, Яндекс.Метрика, Google Analytics
  - Определение наличия/отсутствия виджетов связи и мессенджеров
  - Решить: Next.js API route или отдельный микросервис
  - См. техническую спецификацию в `docs/PLG_STRATEGY.md`
- [ ] Создать дизайны 4 простых виджетов (Simple Widget)
- [ ] На основе Simple Widget создать дизайн Smart Widget (для демо на странице генератора)

### 🟡 Medium Priority (следующие):
- [ ] **Сгенерировать реальные типы Supabase** (сейчас placeholder!)
  - См. инструкции: `packages/database/README.md`
  - Команда: `npx supabase login` → `pnpm db:generate`
  - Или через Dashboard: https://supabase.com/dashboard/project/rhdvlmhcfdqbqjgmcgcc
- [ ] Настроить Supabase Auth для сохранения конфигураций
- [ ] Добавить экспорт в PDF для Cookie Generator
- [ ] Реализовать Simple Widget (базовый чат)

### 🟢 Low Priority (потом):
- [ ] Landing page с GSAP анимациями
- [ ] Интеграция с API Ильи (AI backend)
- [ ] SEO оптимизация для генераторов

### ✅ Выполнено (последние 3-5):
- [x] **Идеальное выравнивание чекбоксов в Step 2 Cookie Generator** (2026-02-08)
  - Выровнены центры чекбоксов с центрами заголовков (оптическое выравнивание)
  - Блок "Технические функции": mt-0.5 для выравнивания по заголовку (игнорируя описание)
  - Блоки "Аналитика/Трансграничная/Реклама": items-center + leading-[1.125rem] для однострочных
  - Заменён кастомный checkbox на native input для "Средства взаимодействия"
  - Результат: все чекбоксы на одном уровне с заголовками
- [x] **Интеграция нового шаблона Cookie Policy в Cookie Generator** (2026-02-08)
  - Создан шаблон документа `cookie-policy.ts` (TypeScript константы)
  - Создан компонент `cookie-policy-form.tsx` (4 блока: технические, аналитика, трансграничная, реклама)
  - Интегрировано в `cookie-generator-client.tsx` и `document-preview.tsx`
  - Step 3 теперь генерирует документ по новому шаблону с подстановкой переменных
  - Удалён старый выбор стиля/тона документа
- [x] Cookie Generator: единый стиль информационных блоков внизу форм (Steps 2-5 приведены к эталону Step 1) (2026-02-08)
- [x] Cookie Generator Steps 2-5: применён Precision Minimalism к формам (консистентность со Step 1) (2026-02-07)
